# 会議のデータがどこへ行くか

この道具が扱うのは**会議の会話そのもの**です（金額・実名・企業間の関係が入ります）。
だから「どこへ何が出るか」を、設定と画面の両方で分かる形にしてあります。

---

## 1. 既定では、何も外へ出ません

初期設定（`config/settings.example.yaml`）は `meeting.external_stt.enabled: false` です。
この状態では、音声もテキストも**あなたの Mac から出ません**（ネットが切れていても動きます）。

## 2. 外へ出す設定にしても、会議のたびに聞きます

`stt.engine` を `deepgram` / `gemini` にしたり、`llm.engine: gemini` にすると、
会議が始まる前に画面が聞きます。

> この会議の「会議中の音声（8〜30 秒ごと）」「会議中の全文と事前資料（要約のため）」を、
> 外（Deepgram ／ Google・プロジェクト `xxx`）へ出しますか?　[外へ出す / 手元で作る]

- **120 秒で「手元」に倒れます**（返事が無い＝承認されていない、として扱います）
- 画面も端末も無いとき（無人実行）は**聞けないので送りません**
- 断っても会議は回ります。手元の Whisper と Ollama に落ちるだけです

## 3. 無料枠では送りません（機械で確かめます）

Gemini API の**無料枠は、送った内容が製品改善に使われ、人間のレビュアーが読むことがあります**
（[追加利用規約](https://ai.google.dev/gemini-api/terms)に「機密情報を送るな」と明記）。有料枠では使われません。

**しかも消せません。** Google の回答は「無料枠のデータは処理前にアカウント・API キー・
プロジェクトから切り離されて非識別化されるため、紐付けも削除もできない」。窓口もありません。

∴ この道具は送る前に `gcloud billing projects describe` で**課金が有効か確かめ**、
確認できないとき（認証切れ・オフライン・gcloud が無い）も**送りません**（fail-closed）。

### Deepgram のとき

Deepgram にも同じ考え方で関門を置いています。見るのは「学習に使われない口か」です。

- 送る要求には毎回 **`mip_opt_out=true`**（モデル改善プログラムから抜ける印）を付けます。
  **設定では外せません**
- この印が付いた要求は、モデル改善に使われないだけでなく、**応答を返したあと保存もされません**
  （zero data retention）
- 会議のはじめに、**その形の要求を Deepgram が実際に受け付けるかを 0.3 秒の無音で 1 回試します**。
  通らなければ音声を送らず、手元に倒れます

**なぜ「設定したか」で守らないか。** プロジェクト側の opt out 設定は、更新しても
**成功の応答が返るのに値が変わらない**ことがありました（実測）。設定を信じていたら、
opt out したつもりで会議の音声を送り続けることになります。関門は「設定したか」ではなく
「**いま実際どうなっているか**」で作ってあります。

## 4. 何が出て、何が出ないか

| | 外へ出るもの | 出ないもの |
|---|---|---|
| 文字起こしを外で回す | **音声**（Deepgram は 8 秒ごと／Gemini は 30 秒ごと・無音は落とす） | 声紋、録音ファイルそのもの |
| 要約を外で回す | **テキスト**（文字起こしと事前資料の本文） | 音声 |
| 画面共有の取り込み | — （**何も出ません**。撮るのも文字を読むのも手元） | 画像、読み取った文字、URL |
| 裏取り（🔍） | **その行と前後 45 秒のテキスト**（Claude CLI、または承認した会議なら Gemini） | 音声、ほかの行 |
| 議事録（会議のあと） | **全文と最終状態のテキスト**（Claude CLI、または承認した会議なら Gemini） | 音声、声紋 |
| どの設定でも | — | 声紋 `speakers.json`・声の台帳 `workspace/voices/`、事前資料の**原本**、画面の操作、録音 |

**話者の割り当ては常に手元**で行います（声紋を外へ出さない設計）。
外に任せるより精度も高いことを実測で確認しています（96% 対 76%）。

### 声の台帳（`meeting.voice_library`）を使うとき

- **声紋は、個人を識別できる情報です**（日本の個人情報保護法では、声の特徴から作ったデータは「個人識別符号」に当たりえます）。
  取引先の声を**会議をまたいで**覚えることになるので、使うかどうかは運用として決めてください
  （社内の規程やプライバシーポリシーでの扱い、相手への説明）
- 台帳は**手元にだけ**置き（`workspace/voices/library.json`・自分だけが読める 600）、**外へは一切出しません**
- 覚えるのは**画面で名前を付けた人**だけで、声紋の数値だけを持ちます（録音の断片は持ちません）
- 外すときは画面「会議アシスタント」の「声の台帳」タブで「台帳から外す」（台帳から外し、ゴミ箱に退避します）

### 画面共有の取り込み（`meeting.screen_capture`）を使うとき

- **相手が見せている資料そのものが、あなたの Mac に残ります**（`workspace/sessions/<会議>/screens/`）。
  取引先の画面を控えることになるので、使うかどうかは運用として決めてください（相手への説明を含めて）
- 撮るのは**設定した会議アプリのウィンドウだけ**です（`owners`）。画面全体・ほかのアプリは撮りません
- 文字の読み取りは **macOS の Vision**（手元）で行います。**外へは画像もテキストも送りません**
- 会議のあとに要らなければ、`screens/` ごと消せます（会議の削除でもまとめて消えます）
- ただし、そのキャプチャの**文字（URL・ページ名）は議事録の材料に並びます**。議事録を外の
  エンジンで作る会議では、その**テキストは外へ出ます**（画像は出ません）

## 5. 送った記録が残ります

外へ出した会議には `workspace/sessions/<会議>/external_sends.jsonl` が残ります
（いつ・どの会議・どのプロジェクトへ・何を）。**後から辿れないのがいちばん困る**ので、
承認のたびに 1 行書きます。

## 6. キーの置き方

- キーは**リポジトリにも設定ファイルにも書きません**。ファイルの場所だけを設定に持ちます
- `chmod 600` で自分だけが読める状態にしてください
- Gemini のキーは `generativelanguage.googleapis.com` だけに制限することを勧めます
- 会議専用の Google Cloud プロジェクトを作ってください（請求が分かれ、事故の範囲も切れます）
- Deepgram のキーも同じで、ファイルに置いて `chmod 600`（`deepgram_key_file`）。
  会議専用のプロジェクトのキーにしてください

## 7. 手元だけで完結させたいとき

```yaml
stt:
  engine: local
llm:
  engine: local
  final_pass: none
minutes:
  engine: local           # auto だと、Claude CLI があればそちらを先に使う
meeting:
  verify_engine: none     # auto だと、Claude CLI があれば 🔍 で行を渡す
  external_stt:
    enabled: false
```

**Claude CLI はテキストだけを渡します**（音声・声紋は渡しません）。サブスクの範囲で動きますが、
「手元の外」であることに変わりはないので、完全にオフラインにしたいときは上のように `local` / `none` に
してください。Ollama も無ければ、議事録は `minutes_prompt.md`（貼り付け用）で止まり、どこへも送りません。

**Deepgram・Gemini は、会議の始めに「外へ出す」を選んだ会議でだけ**使います。Gemini は
議事録・裏取りにも使いますが、その会議で断った・答えなかった場合は、議事録のために
改めて外へ出すことはしません。

## 8. 脆弱性を見つけたら

公開の Issue ではなく、リポジトリの管理者へ直接お知らせください。
内容によっては修正します（修正や返答をお約束するものではありません）。

## 9. 最後に — 責任の所在

この道具を動かすかどうか、どの会議に使うか、どこまで外へ出すかは、**すべて使う人の判断**です。
録音・文字起こしの欠落や誤り、外部サービスへの送信とその費用、機密情報の取り扱いを含め、
**この道具を使ったことで生じたいかなる損害についても、当方は責任を負いません**。
無料のサポート窓口はありません（[README のサポートについて](README.md#サポートについて)）。
