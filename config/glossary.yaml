# 文字起こしの結果を決まった表記へ置き換える辞書（手元の Whisper 用）。
# `誤り: 正しい` の形で書く。自分の会議に出てくる語だけを入れる（多すぎると効果が薄れる）。
# エンジンごとに癖が違うので、辞書も分ける（同じ語でも当たる場所が変わる）。
replacements:
  ワードプレス: WordPress
  ジーエーフォー: GA4

# 置き換えてはいけない語（部分一致で守る）。
# 例: 「クラウド」→「Claude」の置き換えが「クラウドファンディング」を壊さないように。
protected:
  - クラウドファンディング
