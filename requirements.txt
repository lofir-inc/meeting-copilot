# setuptools<81 は必須。81 で pkg_resources が削除され、
#   resemblyzer → webrtcvad → pkg_resources が ModuleNotFoundError になる（2026-09-08 実測）。
setuptools<81

sounddevice>=0.5.0
numpy>=1.26
mlx-whisper>=0.4.0
httpx>=0.27
# 会議中の文字起こしを外（gemini-3.5-transcribe-live）で行うときだけ使う。
#   Live の口は WebSocket（bidiGenerateContent）で、httpx は ws を話せない。
websockets>=13
fastapi>=0.115
uvicorn>=0.30
pyyaml>=6.0
resemblyzer>=0.1.4
silero-vad>=5.1
# silero-vad 6.2.2 から、読み込むだけで onnxruntime を import する（依存には書かれていない）。
#   社内の venv には 6.2.1 が入っていたので気づかなかった。まっさらな環境に入れると
#   会議のあとの作り直しが `ModuleNotFoundError: onnxruntime` で落ちる（2026-09-20 の受け入れ確認で実測）。
onnxruntime>=1.17
pyobjc-framework-ScreenCaptureKit>=12.2
pyobjc-framework-CoreMedia>=12.2
pyobjc-framework-Quartz>=12.2
pyobjc-framework-libdispatch>=12.2
