# しくみ — 何を使い、どう流し、なぜそう決めたか

この道具は「会議の音声」を「その場で読める文字」と「あとで使える議事録」に変えます。
**同じ仕事を、手元だけでも、外のモデルでも回せる**ように、差し替え口を 2 つ（文字起こし・要約）
持っているのが特徴です。

以下の数字はすべて**実測**です（2026-09-11〜09-18／実会議 7 本・のべ 9.5 時間＋公開データでの比較）。

---

## 1. 全体の流れ

```
 自分のマイク ──┐                      ┌── VAD（無音で切る）──┐
                ├─ 2 系統で同時に録る ──┤                      ├─ 文字起こし ─┐
 相手の声      ─┘  （時刻を揃える）     └── 話者の割り当て ────┘              │
 （システム音声）                              （声紋・手元）                   │
                                                                               ▼
       画面（2 分ごとに更新）◀── 状態の差分更新 ◀── 2 分ごとの窓 ◀── 文字起こしの行
                                （決定事項・TODO・論点）
                                        │
                                        ├─ 会議中: 「いま見直す」「裏取り 🔍」「事前資料」
                                        └─ 終了時: 録音から作り直し → 議事録（minutes.md）
```

**2 系統で録るのが土台です。** 自分の声（マイク）と相手の声（システム音声）を別々に録るので、
「自分の発言」は判定せずに確定でき、話者判定は相手側だけでよくなります。会議アプリにも依存しません
（Zoom・Meet・Teams・Webex いずれも同じ）。

---

## 2. 音声の取り込み

| | 使うもの | なぜ |
|---|---|---|
| 相手の声 | **ScreenCaptureKit**（macOS 12.3+） | 会議アプリの音を OS ごと拾える。仮想オーディオ機器の設定が要らない |
| 自分の声 | **sounddevice**（CoreAudio） | 機器名の候補から自動で選ぶ（番号は挿すたびに変わるので設定に書かない） |
| 予備 | BlackHole（ループバック） | ScreenCaptureKit が使えない環境向け |

**時刻合わせ**: 2 系統は開始がずれるので、最初のフレームが届いた時刻からオフセットを測り、
両方の時計を録音の先頭に揃えます。ここを外すと、議事録の並びが会話とずれます
（実測: 揃えないと 71 秒・502 秒ずれた回があった）。

---

## 3. 切り出し（VAD）

**Silero VAD**（`silero-vad`）で、発話のかたまりに切ります。

| 設定 | 既定 | 意味 |
|---|---|---|
| `speech_threshold` | 0.5 | 発話とみなす確率 |
| `silence_duration_sec` | 0.5 | この長さの無音で切る |
| `min_chunk_sec` | 0.4 | これより短い断片は捨てる |
| `max_chunk_sec` | 10 | 長い独話も 10 秒で強制的に切る（話者交代の取りこぼしを防ぐ） |
| `pre_roll_sec` | 0.2 | 語頭が欠けないよう、少し前から含める |

**切り方が後ろの工程すべてを決めます。** 長すぎると話者が混ざり、短すぎると声紋が判定できません
（声紋には最低 3 秒ほどの材料が要る）。

---

## 4. 話者の割り当て — **常に手元**

**Resemblyzer**（256 次元の話者埋め込み・CPU・17 MB）を使います。**外のモデルには任せません。**

| 組み合わせ | 話者の一致率（実測・50 分の実会議） |
|---|---|
| クラウドの文字起こし＋クラウドの話者分け | 76% |
| 手元の文字起こし＋手元の声紋 | 93% |
| **クラウドの文字起こし＋手元の声紋** | **97%** |

**クラウドは「区間の切り方」は上手いが「誰か」は弱い**、というのが実測の結論です。だから
**文字だけを外に出し、誰が言ったかは手元で決めます**（声紋そのものは 1 バイトも外に出ません）。

しくみ:

- 登録済みの声との**コサイン類似度**が閾値（既定 0.75）を超えたらその人
- 超えなければ「不明話者プール」に溜め、一定の発話量（6 秒・2 発話）を超えたら `不明話者N` として昇格
- **画面から名前を付けると、その声を「錨」として登録**し、以後と**過去の行**に反映する
- クラスタが割れたときは、両方に中身がある場合だけ高い閾値（0.90）で併合する

**落とし穴**: `不明話者1` という表示は「覚えた話者」とは限らず「判定できなかったときの置き名」の
こともあります。置き名に名前を付けても声紋は学ばないので、**名前を付けたら必ずその声を錨にする**
実装が要ります（これを外して、実会議で 736 行の名前が消えました）。

---

### 4-1. 前にも出た人（声の台帳・任意）

会議のあとに、名前の付いた人の声を**会議ごとに 1 つの平均**として手元の台帳へ覚えます（1 人あたり最大 10 会議分。
長さ 3 秒以上の行を最大 60 件。平均から外れた行は落とし、そろわなければ「複数人が混ざっている」として覚えない）。
次の会議では、「不明話者」の声が 2 件以上に育った時点で台帳と比べ、近い人を**候補として**出します。

**自動で名前を付けない理由（実測）**

| | 類似度（resemblyzer） |
|---|---|
| 同じ会議の中の本人（前半で覚えて、後半の最初の 2〜5 発話） | 0.95〜0.99 |
| **別の会議の本人**（8 日あけた 2 回の会議） | **0.85〜0.92** |
| **別の会議の別人** | **〜0.85**（8 人・94 組のうち 0.80 以上が 10〜19 組） |

同じ会議の中だけで測ると「0.9 で自動に付けてよい」に見えますが、会議をまたぐと本人と別人が重なります。
人の声を 1 つに平均せず、**いちばん近い会議の声**で比べます（会議ごとにマイクや回線が違うため）。
候補を押した／断ったは `voice_matches.jsonl` に残り、溜まれば「自動でよい近さ」を実データで決められます。

## 5. 文字起こし — 手元／クラウドの 3 経路

**どれを使っても精度はほとんど同じです**（§8）。変わるのは速さ・費用・データの行き先で、
会議ごとに画面で選べます。

| | 窓 | 画面に出るまで | 費用 |
|---|---|---|---|
| 手元（mlx-whisper） | 無音で切る（最大 10 秒） | 数秒〜10 秒。ただし**混んでくると遅れる**（実測 17 分） | ¥0（GPU を使う） |
| Deepgram（`nova-3`） | **8 秒** | 約 9.5 秒 | ¥39/時間 |
| Gemini（`gemini-3.5-transcribe`） | 30 秒 | 約 43 秒 | ¥83/時間 |

窓の長さはエンジンごとに持ちます（`live.window_sec: auto`）。30 秒は**手元の Whisper の都合**で
決めた値で、外のエンジンに引き継ぐ理由がありませんでした。Deepgram は 8 秒でも 30 秒と同じ精度です
（実測・会話 13.1 分: 8 秒 39.8% / 30 秒 39.8% / 5 秒 43.2%）。**5 秒から崩れます。**

### 5-1. 手元（オフライン・無料）

**mlx-whisper**（`whisper-large-v3-turbo`・Apple Silicon の GPU）。VAD で切った区間を 1 つずつ渡します。

- 幻聴対策: 無音で出る定型句（「ご視聴ありがとうございました」等）を捨てる
- 自信の低い出力（`avg_logprob < -1.0`）を捨てる
- **話者名をヒント（initial_prompt）に入れない**。実測で、渡した名前が本文に 118 回紛れ込みました

### 5-2. クラウド・Gemini（`gemini-3.5-transcribe`）

**30 秒ごとにまとめて投げる**「準リアルタイム・バッチ」方式です。ストリーミング版もありますが、
比較の結果こちらを採りました（§8）。

```
VAD の区間を溜める（30 秒ぶん）→ 無音を落として 1 本に繋ぐ → mp3（64 kbps モノラル）
   → Files API にアップロード → POST /v1beta/interactions → 語ごとの時刻つきで返る
   → 区間の対応表で「会議の時計」に戻す → 手元の声紋で話者を当てる → 画面へ
```

実装上の要点（どれも実測で決めたもの）:

- **切れ目は必ず VAD の無音に置く**（語の途中で切らない）
- **無音は送らない**。課金は音声の長さで決まるので**送る音声が約半分**になる
  （65 分の録音 → 実際に送ったのは 30 分ぶん）。無音混じりを投げると**何も返さない**ことがある
- 繋いだせいで消える「話者交代の間」を補うため、**語のまま受け取り、繋ぎ目で必ず行を分ける**
- **最初の窓だけ 10 秒**で締める。30 秒だと最初の 1 行まで 40 秒かかり、録れているか不安になる
- 送れなかった窓は `external_gaps.jsonl` に残し、**会議のあとにその区間だけ**拾い直す
- 専用モデルは `generateContent` では**何も返しません**。入口は `/v1beta/interactions`（実測で確定）

**遅延**: 最初の 1 行まで約 23 秒、以後は平均 28 秒・最大 43 秒（窓 30 秒＋往復 9〜13 秒）。

### 5-3. クラウド・Deepgram（`nova-3`）

Gemini と**同じ形**（同じ関数・同じ刻み方）にしてあるので、差し替えるだけで経路がそのまま動きます。
違うのは次の 3 点です。

- **窓が 8 秒**。往復が速いため（実会議 98 窓の中央で 1.5 秒）、**画面に出るまで約 9.5 秒**
- **話者分けは使いません**（`diarize: false`）。話者は**手元の声紋**で当てたほうが当たります
  （実測 97% 対 76%）。声紋を外へ出さない設計とも合います
- **要求ごとに `mip_opt_out=true`** を付けます。付けた要求はモデル改善に使われず、**保存もされません**。
  会議のはじめに、その形の要求を実際に受け付けるかを 0.3 秒の無音で試してから送ります（§9）

プロジェクト側の opt out 設定は、更新しても**成功が返るのに値が変わらない**ことがありました。
関門は「設定したか」ではなく「**いま実際どうなっているか**」で作っています。

---

## 6. 要約（会議中の状態更新）

2 分ごとに、**差分だけ**を JSON Schema で返させます（全文を毎回渡しません）。

```
プロンプト = 事前資料のまとめ（先頭 2,000 字）＋ 現在の状態 ＋ 直近 2 分の発話
返り     = 差分（新しい決定事項・TODO・質問・論点の遷移）
```

**もとは 30 秒でした**（2026-09-18 に変更）。30.2 分の会議で測ると、**53 回のうち 31 回（58%）は
何も変わらず**、1 回 930 字のうち 759 字（82%）は状態と指示の再送でした。2 分にしても拾う件数は
減らず、送る量が 42% になりました（3 分では後半を拾い損ねます）。設定で 30〜180 秒に変えられます。

| | 手元 | クラウド |
|---|---|---|
| モデル | Ollama `gemma4` | `gemini-3.8-flash` |
| 1 回の所要 | 3.5 秒（GPU が空いていれば） | 1.8 秒 |
| 決定事項の拾い（同じ入力・100 窓） | 2 件 | **29 件** |
| 費用 | ¥0 | 65 分で約 52 円 |

手元のモデルは**文脈が狭い**（8k）ので、事前資料も直近の発話も詰め込めません。クラウド側は 1M。
`thinking_budget: 0`（思考を切る）。要約に推論は要らないのに、入れると出力課金が 4 倍になりました。

---

## 7. 事前資料・裏取り・議事録

### 事前資料（会議ごとに空から）

PDF・Word・Excel・PowerPoint を画面へ落とすと、**手元で本文を取り出します**
（PDF は macOS の PDFKit、Office 系は zip+XML を直接読む＝追加の依存なし）。

長い資料は**足したときに 1 回だけ**まとめます。実物の提案書 PDF は 102,207 字あり、
毎窓に入る 2,000 字では資料の 2% しか見えません。かといって全文を毎窓に載せると
30 秒ごとに課金され、手元のモデルでは文脈にも入りません。**入口で 1 回圧縮する**のが答えでした。

### 事前資料の固有名詞（この会議だけの守り札）

資料から固有名詞（社名・製品名・略称）を手元で拾い、3 か所で使います。

1. **置き換え辞書の守り札**（その会議だけ）: 辞書が固有名詞の一部を壊さないように。実会議で、社名「MDクラウド」が
   辞書の「クラウド→Claude」で「MDClaude」になっていた。画面で付けた名前（「エムディクラウド 参加者C」）も同じ扱い
2. **会議中の要約**: 「表記はこれにそろえる」として毎窓のプロンプトに 300 字以内で載せる
3. **議事録の材料**: `minutes_input.md` に節として並べ、誤変換をその表記へ寄せる手がかりにする（元の表記は括弧で残す）

外の文字起こし（Gemini）には語彙を渡せません（語彙指定とタイムスタンプを同時に使えない）。だから
「起こす前に教える」ではなく「起こしたあとで守る・寄せる」に使います。
拾う順は確かさの順: 「## 用語」の箇条書き → 長い資料をまとめた LLM の terms → 会社名 → 大文字や数字を含む英字
→ ひらがなを含まない短い「」→ 2 回以上出る長いカタカナ（カタカナは一般語が多いので最後）。最大 20 語。

### 裏取り（🔍）

気になった行を押すと、検索して**実際に使われたページ**を出典にして「一致／要確認／不明」を返します（20〜40 秒）。
送るのは**その行と前後 45 秒のテキストだけ**です。手段は上から選びます（`meeting.verify_engine: auto`）。

| 順 | 手段 | 出典の取り方 |
|---|---|---|
| 1 | Claude CLI（`claude -p --allowedTools "WebSearch WebFetch"`） | 検索し、**ページを開いて**確かめた URL |
| 2 | Gemini＋`google_search`（**その会議で外への送信を承認したときだけ**） | `groundingMetadata` の出典＝検索で実際に使われたページ（本文にモデルが書いた URL は捨てる） |
| 3 | なし | 🔍 を薄く出し、押すと理由を返す |

**Gemini は、検索を付けても検索しないことが多い**（実測 2026-09-15・gemini-3.8-flash: 思考 0 では 0 回、思考おまかせでも 7 回中 2 回。
3.5-flash は 3 回中 0 回。聞き直しても検索しないことが多く、82 秒かかったので聞き直しはしない）。検索しなかった答えは
「一致」にせず、画面に「検索されず、記憶で答えました」と出します。**裏取りを当てにするなら Claude CLI を使ってください。**

Claude CLI を先にする理由: サブスクの範囲で動き（従量なし）、**ページを開いて**確かめられるから。
Gemini API は `google_search` を明示的に付けないと**検索せず記憶で答えます**（付けると 5,000 回/月を
超えて $14/1,000 回）。**記憶の URL は出典にしない**（実測で 29 本中 7 本が 404）。

### 議事録

```
会議中の状態（30 秒ごと）→ 録音から全文を作り直す → minutes_input.md → 議事録（minutes.md）
                                                   （話者つき全文＋最終状態）
```

会議のあとに**全文を 1 回だけ読み直す**のが要点です。30〜60 秒の窓では**数分にまたがる合意が見えません**
（実測で、会議中の要約が決定事項 0 件だった回に、全文を読み直すと 16 件見つかりました）。
以前は「全文の最終パス」と「議事録づくり」で全文を 2 回読んでいたので、議事録づくりの 1 回に寄せました
（`llm.final_pass: none`）。

議事録を作る手段は、手元にあるものから上に選び、失敗したら次へ落ちます（`minutes.engine: auto`）。

| 順 | 手段 | 要るもの | しくみ |
|---|---|---|---|
| 1 | Claude CLI | Claude のサブスク | 指示書＋全文を 1 回で渡す（従量なし） |
| 2 | Gemini | API キー＋有料枠＋**その会議の承認** | 同上（文脈 1M なので 1 回で入る） |
| 3 | Ollama | 手元の LLM | 文脈が狭いので、全文を 12,000 字ずつに分けて要点を取り（map）、最後にまとめる（reduce） |
| 4 | 貼り付け用の指示書 | 何も要らない | `minutes_prompt.md`＝指示書＋全文。人が好きな AI チャットに貼る |

**どの環境でも議事録まで行ける**ことを優先しました。4 は「作れなかった」ではなく、
人の手 1 回で済む逃げ道です。指示書（`prompts/minutes.md`）は全手段で同じものを使い、
「全文に無いことを足さない」「金額・日付・社名は全文の表記のまま」「会議中の状態と食い違えば全文を正とする」を守らせます。

---

### 置き換え辞書の候補（使うほど誤りが減る）

会議のあとに、辞書の**候補**を 3 つの出どころから出し、人がチェックしたものだけを辞書へ足します。

| 出どころ | しくみ | 費用 |
|---|---|---|
| 議事録の LLM | 議事録の指示書の末尾で「誤変換らしい語」を JSON で書かせ、本文から切り離す。**全文に一字違わず出てくる語だけ**残す（出てこない語は作り話） | 議事録と同じ 1 回の呼び出し |
| 知っている語に近い表記 | 参加者名・事前資料の用語・辞書の置き換え先を正とし、全文の中の近い表記を拾う（片方がもう片方を含む組は別の語として捨てる） | 手元・LLM なし |
| 辞書が壊した跡 | 置き換え先（英字）に英数字がくっついた語を探し、元の語を守り札の候補にする | 手元・LLM なし |

実会議（09-14・30 分）で試すと、LLM は「レブノル／マイレブノ／レブノー」を 1 つの製品名に寄せ、
辞書が社名を壊した跡（MDClaude）も見つかりました。一方で「経団連→経営層」のような**文脈の直し**も混ざるので、
自動では入れず人が選びます。

## 8. なぜこの構成にしたか（選定の実測）

### 会議中の文字起こしをどうするか（4 案を実測して比較）

| 方式 | 取りこぼし | 画面に出るまで | GPU | 月額（8.5 本） |
|---|---|---|---|---|
| 手元のみ | 6.4% | **17 分遅れ** | 2 つ | ¥0 |
| クラウド・ストリーミング | 3.6% | 0.3 秒 | 1 つ | ¥2,000〜3,500 |
| **30 秒バッチ（採用）** | **0.0〜0.5%** | 約 28 秒 | 1 つ | **¥500 前後** |

**いちばん効く指標は「食い違い率」ではなく「取りこぼし」**です。食い違いは言い回しの差を含みますが、
取りこぼしは**議事録から消えた内容**そのものだからです。
ストリーミングは**セッションが約 9 分で切られ**、再接続の繋ぎ目で発話が落ちました。
30 秒バッチにはその問題がなく、費用も 0.56 倍です。

### どのエンジンで文字起こしするか（公開データで比較）

**文字の食い違い率**（低いほどよい・2026-09-18）:

| エンジン | 朗読（Common Voice 120 本） | 会話（YODAS 120 発話） | 速さ | 費用 |
|---|---|---|---|---|
| 手元の Whisper（large-v3-turbo） | **13.0%** | 43.0% | 50 倍速 | ¥0 |
| Gemini 3.5 transcribe | 13.3% | 39.2% | 27 倍速 | ¥83/時間 |
| Deepgram `nova-3` | 13.3% | **38.2%** | **252 倍速** | ¥39/時間 |

**精度は三者ほぼ同じで、差は速さと費用です。** だから「どれが賢いか」ではなく
「**速さ・費用・データの行き先**」で選んでよい、という結論になりました。
朗読より会話のほうが倍以上むずかしく（同時発話・言い淀み・雑音）、**朗読データだけで選ぶと外します**。

自分の音源で測り直せます: `python scripts/bench_asr.py --audio <音声> --reference <正解>`
（実会議の音声を外のエンジンへ渡そうとすると止まります）。

### 精度をどう測ったか

**別経路で同じ会議を録っている文字起こし（MacWhisper）を「基準」に置き、音声の相互相関で時刻を
合わせて**比べます。基準の置き方で結論がひっくり返ることに注意してください——正解を Whisper 系に
置くと、同じ Whisper 系が構造的に有利になります。台本が分かる合成音声で測ると順位が逆転しました。

---

## 9. 外へ出すときの関門

音声とテキストを外に出す経路には、**4 つの関門**があります（詳しくは [SECURITY.md](SECURITY.md)）。

1. 既定はオフ　2. 会議ごとに画面で聞く（返事が無ければ送らない）
3. **その口が「学習に使われない」ことを機械的に確かめる**（確かめられなければ送らない＝fail-closed）
4. 送った記録を残す

3 番目に見るものはエンジンで違います。**Gemini は課金が有効なプロジェクトか**
（無料枠は送信内容が製品改善に使われ、しかも削除できないため）。**Deepgram は
モデル改善から抜ける印を毎回付けているか、そしてその要求を実際に受け付けるか**です。

---

## 10. 限界（正直なところ）

- **macOS 専用**（相手の音声の取り込みが ScreenCaptureKit）。Apple Silicon 推奨
- 話者の一致は 96〜99%。**会議の冒頭は声紋が育っておらず弱い**
- 手元の文字起こしは物音を言葉にすることがある（自信の低い出力を捨てても 0 にはならない）
- **会話の文字起こしは、どのエンジンでも 4 割近く食い違う**（§8）。読める議事録になるのは、
  辞書・事前資料・話者の割り当て・会議のあとの作り直しを重ねた結果であって、素の精度ではない
- 会議中の要約は「下書き」。議事録は**会議のあとに作り直したもの**を使う
- スピーカーで聞くと自分のマイクに回り込み、同じ発言が二重に文字起こしされる
